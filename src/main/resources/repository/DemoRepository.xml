<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enrich.demo.DemoRepository">

  <select id="findAll" resultType="com.enrich.demo.response.DemoResponse">
    SELECT * FROM demo
  </select>

  <insert id="insert" parameterType="com.enrich.demo.DemoEntity">
    INSERT INTO demo () VALUES ()
  </insert>

</mapper>